import {input} from './utils.js'

function main(){
    const Rendimento_alcool = Number(input('rendimento do carro com álcool(km/l): '))
    const Rendimento_gasolina = Number(input('rendimento do carro com gasolina(km/l: '))
    const distancia = Number(input('Distância a ser percorrida em km: '))
    const litro_alcool = Number(input('valores do litro de alcool:  '))
    const litro_gasolina = Number(input('valor do litro de gasolina: '))
}main()
function calcular_gasto(v1, v2, v3, v4, v5){
    
}