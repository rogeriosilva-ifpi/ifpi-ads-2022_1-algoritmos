import prompt from "prompt-sync"
const input = prompt()

function main(){
const valor = Number(input("Salário da pessoa: "))
let inflacao = Number(input("valor da inflação no período: "))
while(inflacao < )



}